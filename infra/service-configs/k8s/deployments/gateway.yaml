apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: prod-services
  labels:
    app: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
        - name: gateway
          image: shipoff/platform-services:gateway-1.0.1
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: gateway-env
            - secretRef:
                name: general-env
            - secretRef:
                name: service-url-env
          resources:
            requests:
              memory: 500Mi
              cpu: 300m
            limits:
              memory: 1Gi
      imagePullSecrets:
        - name: shipoff-registry-secret
        
---

apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: prod-services
spec:
  selector:
    app: gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
  type: NodePort
